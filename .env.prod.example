# Copy this to .env on the VPS, next to docker-compose.prod.yml, and fill in real values.
# This file stays on the server only — never commit real secrets to git.

DB_USER=chat
DB_PASSWORD=chat_password
DB_NAME=chatdb

# Your frontend's public URL (used for backend CORS)
CORS_ORIGIN=http://172.232.228.90
